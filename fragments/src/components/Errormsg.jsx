const Errormsg = ({ item }) => {

  return (
    <>
      {item.length === 0 && <h3>i am Still Hungryyy...</h3>}
      {/* {fooditem.length === 0 ? <h3>I Am Hungry....</h3> : null} */}
    </>
  )
}

export default Errormsg;